// TodoItem
import React, { PureComponent } from 'react';

const Child = (props) => {
  console.log('child rendering');
  // HTML template

  // props.setItems()

  return (
    <h1>CHILD</h1>
  );

};

export default Child;
