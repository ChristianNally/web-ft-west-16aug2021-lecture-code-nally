// TodoItem
import React from 'react';

const Child = (props) => {
  console.log('child rendering');
  // HTML template

  // props.setItems()

  return (
    <span>CHILD</span>
  );

};

export default Child;
